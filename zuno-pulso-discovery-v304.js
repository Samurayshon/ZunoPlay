(()=>{
if(window.__ZUNO_PULSO_DISCOVERY_V304__)return;window.__ZUNO_PULSO_DISCOVERY_V304__=true;
const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
let client=null,user=null,mode='for-you',run=0;
const timeout=(p,ms=5000)=>Promise.race([Promise.resolve(p),new Promise((_,rej)=>setTimeout(()=>rej(new Error('timeout')),ms))]);
function isDiscover(){return $('.zm-tab[data-view="plaza"]')?.classList.contains('active')}
async function getClient(){if(client)return client;for(let i=0;i<35;i++){if(window.ZunoSupabaseClient){client=window.ZunoSupabaseClient;break}await new Promise(r=>setTimeout(r,80))}if(!client)throw new Error('client-unavailable');const s=await timeout(client.auth.getSession(),2500);user=s?.data?.session?.user||null;if(!user)throw new Error('session-unavailable');return client}
function ensure(){let root=$('#zpDiscoveryV304');if(root)return root;root=document.createElement('section');root.id='zpDiscoveryV304';root.className='zp304-discovery';root.hidden=true;root.innerHTML='<nav class="zp304-nav" aria-label="Descobrir"><button class="active" type="button" data-zp304-mode="for-you"><span>✦</span><div><b>Para você</b><small>Publicações com mais engajamento</small></div></button><button type="button" data-zp304-mode="zuno"><span>◇</span><div><b>Descoberta Zuno</b><small>O que movimenta o ZunoPlay</small></div></button></nav><div id="zp304Panel" class="zp304-panel"></div>';const header=$('.zm-header');header?.insertAdjacentElement('afterend',root);$$('[data-zp304-mode]',root).forEach(b=>b.addEventListener('click',()=>setMode(b.dataset.zp304Mode)));return root}
function setActivePrimary(){const p=$('.zm-tab[data-view="moments"]'),d=$('.zm-tab[data-view="plaza"]');p?.classList.remove('active');d?.classList.add('active');const app=$('#momentsApp');if(app){app.dataset.zpView='general';app.dataset.zpDiscoveryMode=mode}$('#composer')?.setAttribute('hidden','');$('#topicStrip')?.setAttribute('hidden','');$('#plazaIntro')?.setAttribute('hidden','');$('#zpDiscoveryHub')?.setAttribute('hidden','');const feed=$('#feed');if(feed)feed.style.display='none';const root=ensure();if(root)root.hidden=false;$('#zpComposeFab')?.setAttribute('hidden','')}
function leave(){run++;const root=$('#zpDiscoveryV304');if(root)root.hidden=true;const feed=$('#feed');if(feed)feed.style.removeProperty('display');const app=$('#momentsApp');if(app){app.dataset.zpView='friends';app.dataset.zpDiscoveryMode=''}$('#zpComposeFab')?.removeAttribute('hidden')}
function loading(){const p=$('#zp304Panel');if(p)p.innerHTML='<div class="zp304-loading"><span></span><b>Preparando o Descobrir</b><small>Buscando novidades do ZunoPlay...</small></div>'}
function fail(title,text){const p=$('#zp304Panel');if(p)p.innerHTML=`<div class="zp304-empty"><b>${esc(title)}</b><span>${esc(text)}</span><button type="button" id="zp304Retry">Tentar novamente</button></div>`;$('#zp304Retry')?.addEventListener('click',load)}
function ago(v){const s=Math.max(0,(Date.now()-new Date(v))/1000);if(s<60)return'agora';if(s<3600)return`${Math.floor(s/60)} min`;if(s<86400)return`${Math.floor(s/3600)} h`;return`${Math.floor(s/86400)} d`}
function score(post,likes,comments){const l=likes.filter(x=>x.post_id===post.id).length,c=comments.filter(x=>x.post_id===post.id).length,h=Math.max(0,(Date.now()-new Date(post.created_at))/36e5);return l*4+c*6+Math.max(0,30-h/3)}
function media(c,p){if(!p.media_path)return'';const u=c.storage.from('moments').getPublicUrl(p.media_path).data.publicUrl;return p.media_type==='video'?`<div class="zp304-media"><video src="${esc(u)}" controls playsinline preload="metadata"></video></div>`:`<div class="zp304-media"><img src="${esc(u)}" loading="lazy" alt=""></div>`}
function card(c,p,profiles,likes,comments){const pr=profiles.get(p.user_id)||{},name=pr.nickname||pr.username||'ZunoPlayer',l=likes.filter(x=>x.post_id===p.id).length,cm=comments.filter(x=>x.post_id===p.id).length,av=pr.avatar_url?`<img src="${esc(pr.avatar_url)}" alt="">`:esc(name[0]?.toUpperCase()||'Z');return`<article class="zp-post zp304-card"><header class="zp-post-head"><button class="zp-author" type="button" data-zp304-profile="${esc(p.user_id)}"><span class="zp-avatar">${av}</span><span><b>${esc(name)}</b><small>Nível ${pr.level||1} · ${ago(p.created_at)}</small></span></button><span class="zp304-score">${Math.round(p.__score||0)} destaque</span></header>${p.content?`<div class="zp-copy">${esc(p.content)}</div>`:''}${media(c,p)}<div class="zp-stats"><span>${l} curtida${l===1?'':'s'}</span><span>${cm} comentário${cm===1?'':'s'}</span></div></article>`}
async function fetchData(){const c=await getClient();const uid=user.id;const postsR=await timeout(c.from('moments_posts').select('*').eq('visibility','public').neq('user_id',uid).order('created_at',{ascending:false}).limit(120),4500);if(postsR.error)throw postsR.error;const posts=postsR.data||[],ids=posts.map(x=>x.id),uids=[...new Set(posts.map(x=>x.user_id))];let likes=[],comments=[],profiles=[];if(ids.length){const [lr,cr]=await timeout(Promise.all([c.from('moments_likes').select('post_id,user_id').in('post_id',ids),c.from('moments_comments').select('post_id,user_id').in('post_id',ids)]),4000);likes=lr.data||[];comments=cr.data||[]}if(uids.length){const pr=await timeout(c.from('profiles').select('id,username,nickname,avatar_url,level').in('id',uids),4000);profiles=pr.data||[]}return{c,posts,likes,comments,profiles:new Map(profiles.map(x=>[x.id,x]))}}
async function loadForYou(my){loading();try{const d=await fetchData();if(my!==run||!isDiscover()||mode!=='for-you')return;const ranked=d.posts.map(p=>({...p,__score:score(p,d.likes,d.comments)})).sort((a,b)=>b.__score-a.__score||new Date(b.created_at)-new Date(a.created_at));const panel=$('#zp304Panel');if(!ranked.length){fail('Nada em destaque por enquanto','Quando novas publicações públicas ganharem atividade, elas aparecerão aqui.');return}panel.innerHTML=`<div class="zp304-section-title"><div><b>Para você</b><small>Ordenado por engajamento e atividade recente</small></div><span>${ranked.length}</span></div><div class="zp304-feed">${ranked.slice(0,40).map(p=>card(d.c,p,d.profiles,d.likes,d.comments)).join('')}</div>`;$$('[data-zp304-profile]',panel).forEach(b=>b.onclick=()=>location.href=`perfil.html?id=${encodeURIComponent(b.dataset.zp304Profile)}`)}catch(e){console.error('[Descobrir v304]',e);if(my===run&&isDiscover())fail('Não foi possível carregar o Descobrir','A conexão demorou mais que o esperado. Tente novamente.') }}
async function loadZuno(my){loading();try{const d=await fetchData();if(my!==run||!isDiscover()||mode!=='zuno')return;const since24=Date.now()-864e5,creators=new Map(),topics=new Map();d.posts.forEach(p=>{const l=d.likes.filter(x=>x.post_id===p.id).length,c=d.comments.filter(x=>x.post_id===p.id).length,e=creators.get(p.user_id)||{posts:0,eng:0};e.posts++;e.eng+=l*4+c*6;creators.set(p.user_id,e);if(new Date(p.created_at).getTime()>=since24)(p.hashtags||[]).forEach(t=>topics.set(t,(topics.get(t)||0)+1))});const ranked=[...creators.entries()].map(([uid,x])=>({uid,...x,pr:d.profiles.get(uid)||{}})).sort((a,b)=>b.eng-a.eng||b.posts-a.posts).slice(0,8),hot=[...topics.entries()].sort((a,b)=>b[1]-a[1]).slice(0,10);const panel=$('#zp304Panel');panel.innerHTML=`<div class="zp304-hero"><small>DESCOBERTA ZUNO</small><h2>O que está movimentando o ZunoPlay</h2><p>Criadores, assuntos e atividade pública que estão ganhando força agora.</p><div class="zp304-metrics"><span><b>${d.posts.length}</b> publicações</span><span><b>${ranked.length}</b> criadores</span><span><b>${hot.length}</b> assuntos</span></div></div><section class="zp304-box"><header><b>Criadores em destaque</b><small>atividade recente</small></header><div class="zp304-creators">${ranked.length?ranked.map(x=>{const n=x.pr.nickname||x.pr.username||'ZunoPlayer',av=x.pr.avatar_url?`<img src="${esc(x.pr.avatar_url)}" alt="">`:esc(n[0]?.toUpperCase()||'Z');return`<button type="button" data-zp304-profile="${esc(x.uid)}"><span>${av}</span><b>${esc(n)}</b><small>${x.posts} publicações</small></button>`}).join(''):'<div class="zp304-note">Ainda não há criadores suficientes para o ranking.</div>'}</div></section><section class="zp304-box"><header><b>Assuntos aquecendo</b><small>últimas 24h</small></header><div class="zp304-topics">${hot.length?hot.map(([t,n])=>`<span>#${esc(t)} <small>${n}</small></span>`).join(''):'<div class="zp304-note">Os assuntos em alta aparecerão aqui.</div>'}</div></section>`;$$('[data-zp304-profile]',panel).forEach(b=>b.onclick=()=>location.href=`perfil.html?id=${encodeURIComponent(b.dataset.zp304Profile)}`)}catch(e){console.error('[Descoberta Zuno v304]',e);if(my===run&&isDiscover())fail('Descoberta Zuno indisponível agora','Tente novamente em alguns instantes.')}}
function load(){const my=++run;if(mode==='zuno')loadZuno(my);else loadForYou(my)}
function setMode(next){mode=next==='zuno'?'zuno':'for-you';const root=ensure();$$('[data-zp304-mode]',root).forEach(b=>b.classList.toggle('active',b.dataset.zp304Mode===mode));const app=$('#momentsApp');if(app)app.dataset.zpDiscoveryMode=mode;load()}
function open(){setActivePrimary();setMode('for-you');try{window.posthog?.capture?.('pulso_discover_open',{controller:'v304'})}catch(_){}}
function clickCapture(e){const d=e.target.closest('.zm-tab[data-view="plaza"]');if(d){e.preventDefault();e.stopImmediatePropagation();open();return}const p=e.target.closest('.zm-tab[data-view="moments"]');if(p)leave()}
function boot(){ensure();document.addEventListener('click',clickCapture,true);if(isDiscover())open()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();