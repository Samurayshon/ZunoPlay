<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Proposal A / Close-up Premium: stronger glow behind the canonical Zuno. -->
    <item
        android:width="108dp"
        android:height="108dp"
        android:gravity="center"
        android:drawable="@drawable/zuno_app_icon_v2_halo" />

    <!-- Reuse the canonical Central mascot unchanged, only enlarging its presentation. -->
    <item
        android:width="118dp"
        android:height="118dp"
        android:gravity="center"
        android:drawable="@drawable/zuno_app_icon_foreground" />
</layer-list>
