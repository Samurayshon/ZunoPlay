(()=>{
if(window.__ZUNO_GAME_PROGRESSION__)return;window.__ZUNO_GAME_PROGRESSION__=true;
const page=(location.pathname.split('/').pop()||'').toLowerCase(),$=(s,r=document)=>r?.querySelector?.(s),sleep=ms=>new Promise(r=>setTimeout(r,ms));let sb=null,user=null;
async function waitClient(){for(let i=0;i<70;i++){if(window.ZunoSupabaseClient)return window.ZunoSupabaseClient;await sleep(100)}return null}
function levelFloor(level){return Math.pow(Math.max(0,level-1),2)*250}function levelCeil(level){return Math.pow(level,2)*250}function xpPct(xp,level){const a=levelFloor(level),b=levelCeil(level);return Math.max(0,Math.min(100,(xp-a)/(b-a)*100))}
async function loadProgress(){const{data}=await sb.from('game_progress').select('*').eq('user_id',user.id).maybeSingle();return data||{xp:0,game_level:1,total_games:0,total_wins:0,best_score:0}}
function cardHTML(p){const level=Number(p.game_level)||1,xp=Number(p.xp)||0,pct=xpPct(xp,level),wins=Number(p.total_wins)||0,games=Number(p.total_games)||0,best=Number(p.best_score)||0;return '<div class="zuno-game-progress-card"><div class="zuno-game-progress-head"><div class="zuno-game-level-orb">'+level+'</div><div class="zuno-game-progress-copy"><b>Nível de Jogos '+level+'</b><small>Zuno Stack é o jogo ativo do ciclo atual</small></div><div class="zuno-game-xp">'+xp+' XP</div></div><div class="zuno-game-xpbar"><i style="width:'+pct+'%"></i></div><div class="zuno-game-progress-stats"><div class="zuno-game-progress-stat"><small>Partidas</small><b>'+games+'</b></div><div class="zuno-game-progress-stat"><small>Vitórias</small><b>'+wins+'</b></div><div class="zuno-game-progress-stat"><small>Recorde</small><b>'+best+'</b></div><div class="zuno-game-progress-stat"><small>Jogo ativo</small><b>STACK</b></div></div></div>'}
function mountHub(p){const anchor=$('.hero')||$('main');if(!anchor||$('#zunoGameProgressHub'))return;const box=document.createElement('section');box.id='zunoGameProgressHub';box.innerHTML=cardHTML(p);anchor.insertAdjacentElement('afterend',box)}
function mountHistory(p){const anchor=$('.card .subtitle')||$('.subtitle')||$('main');if(!anchor||$('#zunoGameProgressHistory'))return;const box=document.createElement('div');box.id='zunoGameProgressHistory';box.innerHTML=cardHTML(p);anchor.insertAdjacentElement('afterend',box)}
async function init(){sb=await waitClient();if(!sb)return;const{data}=await sb.auth.getSession();user=data?.session?.user||null;if(!user)return;const progress=await loadProgress();if(page==='jogos.html')mountHub(progress);if(page==='historico.html')mountHistory(progress)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>init().catch(console.warn),{once:true});else init().catch(console.warn);
})();