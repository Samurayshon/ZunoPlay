name: Preview Backend Identity

on:
  pull_request:
    branches: [main]
    paths:
      - 'docs/ZUNOPLAY_PREVIEW_BACKEND.md'

jobs:
  backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          grep -q 'lqymrmionvvfbgfrdryo' docs/ZUNOPLAY_PREVIEW_BACKEND.md
          grep -q 'rliymfbbhqoejgfvsbuu' docs/ZUNOPLAY_PREVIEW_BACKEND.md
          grep -q 'Não aceitar fallback silencioso' docs/ZUNOPLAY_PREVIEW_BACKEND.md
