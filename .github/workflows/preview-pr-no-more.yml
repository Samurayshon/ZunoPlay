name: Preview PR No More
on:
  pull_request:
    branches: [main]
jobs:
  stop:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: grep -q 'No more files. Create PR.' docs/ZUNOPLAY_PREVIEW_PR_NO_MORE.md
