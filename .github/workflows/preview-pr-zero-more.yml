name: Preview PR Zero More
on:
  pull_request:
    branches: [main]
jobs:
  freeze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: grep -q 'Zero more auxiliary changes before PR' docs/ZUNOPLAY_PREVIEW_PR_ZERO_MORE.md
