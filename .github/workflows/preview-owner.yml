name: Preview Owner Contract

on:
  pull_request:
    branches: [main]
    paths:
      - 'docs/ZUNOPLAY_PREVIEW_OWNER.md'

jobs:
  owner:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: grep -q 'Gerente/orquestrador: ChatGPT' docs/ZUNOPLAY_PREVIEW_OWNER.md
