name: Preview PR End
on:
  pull_request:
    branches: [main]
jobs:
  end:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: grep -q 'END. PR NEXT.' docs/ZUNOPLAY_PREVIEW_PR_END.md
