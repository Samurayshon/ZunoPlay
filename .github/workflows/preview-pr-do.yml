name: Preview PR Do
on:
  pull_request:
    branches: [main]
jobs:
  do:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: grep -q 'DO PR.' docs/ZUNOPLAY_PREVIEW_PR_DO.md
