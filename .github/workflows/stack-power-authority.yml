name: Zuno Stack Power Authority

on:
  pull_request:
    branches: [main]
    paths:
      - 'zuno-stack-power-authority.js'
      - 'zuno-stack-server-engine-guard.js'
      - 'zuno-stack-performance-official.js'
      - 'supabase/migrations/*zuno_stack_*powers.sql'
      - 'supabase/migrations/*zuno_stack_gelo.sql'
      - 'supabase/migrations/*zuno_stack_desfazer.sql'
      - '.github/workflows/stack-power-authority.yml'
  push:
    branches: [main]
    paths:
      - 'zuno-stack-power-authority.js'
      - 'zuno-stack-server-engine-guard.js'
      - 'zuno-stack-performance-official.js'
      - 'supabase/migrations/*zuno_stack_*powers.sql'
      - 'supabase/migrations/*zuno_stack_gelo.sql'
      - 'supabase/migrations/*zuno_stack_desfazer.sql'
      - '.github/workflows/stack-power-authority.yml'

jobs:
  power-authority:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Guard server power authority
        shell: bash
        run: |
          set -euo pipefail
          node --check zuno-stack-power-authority.js
          node --check zuno-stack-server-engine-guard.js
          base='supabase/migrations/20260830034449_server_authoritative_zuno_stack_fluxo_troca.sql'
          board='supabase/migrations/20260830035506_server_authoritative_zuno_stack_board_powers.sql'
          gelo='supabase/migrations/20260830040257_server_authoritative_zuno_stack_gelo.sql'
          desfazer='supabase/migrations/20260830041009_server_authoritative_zuno_stack_desfazer.sql'
          test -s "$base"
          test -s "$board"
          test -s "$gelo"
          test -s "$desfazer"
          grep -q 'zuno_stack_server_power_loadout' "$base"
          grep -q 'trg_zuno_stack_power_state_guard' "$base"
          grep -q "p_power not in ('fluxo','troca','explosion','elo','fase','vortice','ima')" "$board"
          grep -q 'zuno_stack_timer_state_guard' "$gelo"
          grep -q 'zuno_stack_apply_gelo_internal' "$gelo"
          grep -q 'zuno_stack_apply_desfazer_internal' "$desfazer"
          grep -q "v_phase='final'" "$desfazer"
          grep -q 'stack_undo_unavailable' "$desfazer"
          grep -q "'undoLeft',v_undo_left-1" "$desfazer"
          grep -q "'{desfazer}'" "$desfazer"
          grep -q 'security invoker' "$desfazer"
          grep -q 'revoke all on function public.zuno_stack_desfazer' "$desfazer"
          grep -q "zuno_stack_gelo" zuno-stack-power-authority.js
          grep -q "zuno_stack_desfazer" zuno-stack-power-authority.js
          grep -Fq '/^(fluxo|troca|explosion|elo|fase|vortice|ima|gelo|desfazer)$/' zuno-stack-power-authority.js
          grep -Fq '/^power_(fluxo|troca|explosion|elo|fase|vortice|ima|gelo|desfazer)$/' zuno-stack-server-engine-guard.js
          grep -q 'ZunoStackPowerAuthority?.request' zuno-stack-server-engine-guard.js
          grep -q "resync('power_server_required')" zuno-stack-server-engine-guard.js
