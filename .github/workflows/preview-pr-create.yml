name: Preview PR Create Marker
on:
  pull_request:
    branches: [main]
jobs:
  marker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: grep -q 'PR creation is the next operation' docs/ZUNOPLAY_PREVIEW_PR_CREATE.md
