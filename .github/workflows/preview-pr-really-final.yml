name: Preview PR Really Final
on:
  pull_request:
    branches: [main]
jobs:
  final:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: grep -q 'Final. Create PR.' docs/ZUNOPLAY_PREVIEW_PR_REALLY_FINAL.md
