name: Preview PR No More Actual
on:
  pull_request:
    branches: [main]
jobs:
  freeze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: grep -q 'NO MORE. CREATE PR.' docs/ZUNOPLAY_PREVIEW_PR_NO_MORE_ACTUAL.md
