name: Preview PR Go
on:
  pull_request:
    branches: [main]
jobs:
  go:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: grep -q 'GO: create the PR' docs/ZUNOPLAY_PREVIEW_PR_GO.md
