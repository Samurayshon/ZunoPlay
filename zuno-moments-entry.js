(()=>{function mount(){const grid=document.querySelector('.menu-grid');if(!grid||grid.querySelector('[data-zuno-moments-entry]'))return;const card=document.createElement('button');card.className='menu-card';card.dataset.zunoMomentsEntry='1';card.style.setProperty('--glow','#d43cff');card.onclick=()=>location.href='pulso.html';card.innerHTML='<div class="menu-icon"></div><h2>Momentos</h2><p>Posts, fotos e Praça</p>';const communities=[...grid.querySelectorAll('.menu-card')].find(x=>x.textContent.includes('Comunidades'));if(communities)communities.after(card);else grid.prepend(card)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',mount,{once:true});else mount();setTimeout(mount,300)})();