import {TRAY_CAPACITY,USE_POWER,createCoreTransitions,createModeRules,createPowerCatalog,createPrng,createRulesContext,createValidatedBoardState,generateBoard,getAvailableTileIds,usePowerTransition} from '../core/index.mjs';import {SOLO_FAMILIES,SOLO_LAYER_COUNTS,SOLO_LAYOUTS,assertFinalBoardContract,createFinalBoardConfig} from './solo-expansion.mjs';export const SOLO_MODE='solo';export const SOLO_RULESET_VERSION='solo-complete-r1';export const SOLO_STATUS=Object.freeze({CREATED:'CREATED',PLAYING:'PLAYING',WON:'WON',LOST:'LOST'});export const SOLO_COMMAND=Object.freeze({UNDO:'SOLO_UNDO',HINT:'SOLO_HINT',RESCUE:'SOLO_RESCUE'});export const SOLO_POWER=Object.freeze({SHIFT:'shift'});export const DEFAULT_SOLO_BOARD=Object.freeze(createFinalBoardConfig(SOLO_LAYOUTS[0]));export const DEFAULT_SOLO_RESOURCES=Object.freeze({undo:3,hint:3,rescue:1,powerShift:2});export function createSoloBoard(config=DEFAULT_SOLO_BOARD,seed='solo-seed'){const base=generateBoard(config,seed),families=[...config.families];if(base.tiles.length%families.length!==0)throw new RangeError('Solo board tile count must be divisible by family count');const perFamily=base.tiles.length/families.length;if(perFamily%3!==0)throw new RangeError('Solo board must provide complete trios per family');const removed=new Set(),removalOrder=[];while(removalOrder.length<base.tiles.length){const next=base.tiles.find(tile=>!removed.has(tile.id)&&(base.blockersByTile[tile.id]??[]).every(id=>removed.has(id)));if(!next)throw new Error('Solo board has no deterministic removal path');removed.add(next.id);removalOrder.push(next.id)}const familyCycle=createPrng(`${seed}|solo-families`).shuffle(families),familyById=new Map();for(let index=0;index<removalOrder.length;index++)familyById.set(removalOrder[index],familyCycle[Math.floor(index/3)%familyCycle.length]);const tiles=base.tiles.map(tile=>({...tile,family:familyById.get(tile.id)})),board=createValidatedBoardState({tiles,layerCount:base.layerCount,meta:{...base.meta,mode:'solo',balancedFamilies:true,guaranteedTrioPath:true,finalExpansion:config.layerCounts.length===9}});if(config.layerCounts.length===9&&config.families.length===9)assertFinalBoardContract(board);return board}function restoreNewestTrayTile(player){if(!player.tray.length)return null;const tileId=player.tray[player.tray.length-1],found=player.board.tiles.some(tile=>tile.id===tileId&&tile.removed);if(!found)return null;return {...player,board:{...player.board,tiles:player.board.tiles.map(tile=>tile.id===tileId?{...tile,removed:false}:tile)},tray:player.tray.slice(0,-1)}}export function createSoloRules(){const powerCatalog=createPowerCatalog([{id:SOLO_POWER.SHIFT,modes:[SOLO_MODE],cost:3,charges:2,precondition:({player})=>(player.resources?.powerShift??0)>0&&player.tray.length>0,apply:({player})=>{const restored=restoreNewestTrayTile(player);if(!restored)return null;return {player:{...restored,resources:{...restored.resources,powerShift:restored.resources.powerShift-1}},events:[{type:'SOLO_POWER_SHIFTED',payload:{restoredTileId:player.tray[player.tray.length-1]}}]}}}]);const transitions={...createCoreTransitions(),[USE_POWER]:usePowerTransition},rules=createModeRules({modeId:SOLO_MODE,playerSlots:1,transitions,powerCatalog});rules.progression={scorePerTrio:100,comboStep:1,comboResetOnPickWithoutTrio:false,pulsePerTrio:1,pulseMax:10,pulseUseCost:1};return rules}export function createSoloRulesContext(){return createRulesContext({rules:createSoloRules(),config:{trayCapacity:TRAY_CAPACITY,resources:{...DEFAULT_SOLO_RESOURCES}}})}export function evaluateSoloStatus(state){const player=state.players[0];if(!player)return SOLO_STATUS.LOST;let remaining=0;for(const tile of player.board.tiles)if(!tile.removed)remaining++;if(remaining===0&&player.tray.length===0)return SOLO_STATUS.WON;if(player.tray.length>=TRAY_CAPACITY)return SOLO_STATUS.LOST;return SOLO_STATUS.PLAYING}export function chooseSoloHint(state){const player=state.players[0];if(!player)return null;const available=new Set(getAvailableTileIds(player.board)),trayFamilies=new Map();for(const tileId of player.tray){const tile=player.board.tiles.find(candidate=>candidate.id===tileId);if(tile)trayFamilies.set(tile.family,(trayFamilies.get(tile.family)??0)+1)}const candidates=player.board.tiles.filter(tile=>!tile.removed&&available.has(tile.id));candidates.sort((a,b)=>(trayFamilies.get(b.family)??0)-(trayFamilies.get(a.family)??0)||b.layer-a.layer||a.id.localeCompare(b.id));return candidates[0]?.id??null}export function rescueSoloPlayer(player){return restoreNewestTrayTile(player)}export {SOLO_FAMILIES,SOLO_LAYER_COUNTS};