import{acceptedTransition,createDomainEvent,rejectedTransition}from'./contracts.mjs';import{canPickTile}from'./board.mjs';import{appendTrayTileId,resolveFirstTrio,TRAY_CAPACITY,validateTrayState}from'./tray.mjs';import{applyNonTrioComboRule,applyTrioProgression}from'./progression.mjs';
export const PICK_TILE='PICK_TILE';function findPlayerIndex(state,actorId){if(!state||!Array.isArray(state.players))return-1;return state.players.findIndex(player=>player?.playerId===actorId)}function reject(state,code,details=null){return rejectedTransition(state,code,details)}
export function pickTileTransition(state,command,context={}){if(!command||command.type!==PICK_TILE)return reject(state,'INVALID_PICK_COMMAND');const tileId=command.payload?.tileId;if(typeof tileId!=='string'||!tileId)return reject(state,'INVALID_TILE_ID');const playerIndex=findPlayerIndex(state,command.actorId);if(playerIndex<0)return reject(state,'PLAYER_NOT_FOUND',{actorId:command.actorId});const player=state.players[playerIndex];validateTrayState(player.board,player.tray);const tile=player.board.tiles.find(candidate=>candidate.id===tileId);if(!tile)return reject(state,'TILE_NOT_FOUND',{tileId});if(tile.removed)return reject(state,'TILE_ALREADY_REMOVED',{tileId});if(!canPickTile(player.board,tileId))return reject(state,'TILE_BLOCKED',{tileId});if(player.tray.length>=TRAY_CAPACITY)return reject(state,'TRAY_FULL',{capacity:TRAY_CAPACITY});const board={...player.board,tiles:player.board.tiles.map(candidate=>candidate.id===tileId?{...candidate,removed:true}:candidate)};const resolution=resolveFirstTrio(board,appendTrayTileId(player.tray,tileId));validateTrayState(board,resolution.tray);let nextPlayer={...player,board,tray:resolution.tray};const events=[createDomainEvent('TILE_PICKED',{actorId:player.playerId,tileId:tile.id,family:tile.family})];if(resolution.resolved){events.push(createDomainEvent('TRIO_RESOLVED',{actorId:player.playerId,family:resolution.resolved.family,tileIds:resolution.resolved.tileIds}));const progression=applyTrioProgression(nextPlayer,context?.rules?.progression);nextPlayer=progression.player;events.push(createDomainEvent('SCORE_CHANGED',{actorId:player.playerId,...progression.changes.score}),createDomainEvent('COMBO_CHANGED',{actorId:player.playerId,...progression.changes.combo}));if(progression.changes.pulse.delta!==0)events.push(createDomainEvent('PULSE_CHANGED',{actorId:player.playerId,...progression.changes.pulse}))}else{const combo=applyNonTrioComboRule(nextPlayer,context?.rules?.progression);nextPlayer=combo.player;if(combo.changed)events.push(createDomainEvent('COMBO_CHANGED',{actorId:player.playerId,previous:combo.previous,current:combo.current}))}events.push(createDomainEvent('TRAY_CHANGED',{actorId:player.playerId,tileIds:[...resolution.tray],size:resolution.tray.length,capacity:TRAY_CAPACITY}));const nextState={...state,players:state.players.map((candidate,index)=>index===playerIndex?nextPlayer:candidate)};return acceptedTransition(nextState,events)}
export function createCoreTransitions(){return{[PICK_TILE]:pickTileTransition}}
