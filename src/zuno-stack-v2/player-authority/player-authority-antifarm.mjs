export const PLAYER_AUTHORITY_ANTI_FARM_POLICY_VERSION='player-authority-antifarm-r1';
export const PLAYER_AUTHORITY_ANTI_FARM_FLAG=Object.freeze({TOO_SHORT:'AUTHORITY_TOO_SHORT',REPEATED_PARTICIPANTS:'AUTHORITY_REPEATED_PARTICIPANTS',ABANDONMENT:'AUTHORITY_ABANDONMENT'});
const signature=players=>[...players].sort().join('|');
export function evaluatePlayerAuthorityAntiFarm(result,history=[],policy={}){const minDuration=policy.minDuration??0;const repeatLimit=policy.repeatParticipantLimit??Infinity;const flags=new Set(result.antiFarmFlags??[]);const duration=result.finishedAt-result.startedAt;if(duration<minDuration)flags.add(PLAYER_AUTHORITY_ANTI_FARM_FLAG.TOO_SHORT);if(Number.isFinite(repeatLimit)){const target=signature(result.players);let repeats=0;for(let i=history.length-1;i>=0;i--){if(signature(history[i].players)===target)repeats+=1;else break;}if(repeats>=repeatLimit)flags.add(PLAYER_AUTHORITY_ANTI_FARM_FLAG.REPEATED_PARTICIPANTS);}if(result.terminalReason==='abandoned'||result.terminalReason==='disconnect_expired')flags.add(PLAYER_AUTHORITY_ANTI_FARM_FLAG.ABANDONMENT);return Object.freeze({policyVersion:PLAYER_AUTHORITY_ANTI_FARM_POLICY_VERSION,flags:Object.freeze([...flags].sort())});}
export function applyPlayerAuthorityAntiFarm(result,history=[],policy={}){const evaluation=evaluatePlayerAuthorityAntiFarm(result,history,policy);return Object.freeze({...result,antiFarmFlags:evaluation.flags,authorityAntiFarmPolicyVersion:evaluation.policyVersion});}
