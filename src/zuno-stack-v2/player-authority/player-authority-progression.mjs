export const PLAYER_AUTHORITY_TIERS=Object.freeze([{level:1,name:'origin',min:0},{level:2,name:'signal',min:25},{level:3,name:'pulse',min:75},{level:4,name:'vector',min:175},{level:5,name:'nexus',min:350}]);
export function derivePlayerAuthorityTier(authority=0){if(!Number.isSafeInteger(authority)||authority<0)throw new TypeError('authority must be a non-negative safe integer');let tier=PLAYER_AUTHORITY_TIERS[0];for(const candidate of PLAYER_AUTHORITY_TIERS)if(authority>=candidate.min)tier=candidate;const next=PLAYER_AUTHORITY_TIERS.find(item=>item.level===tier.level+1)??null;return Object.freeze({level:tier.level,tier:tier.name,authority,currentMin:tier.min,nextMin:next?.min??null,progress:next?authority-tier.min:0,progressRequired:next?next.min-tier.min:0});}
export function projectPublicPlayerAuthority(player){const safe=player??{playerId:null,authority:0,matches:0,byMode:{solo:0,trio:0,pvp:0}};const progression=derivePlayerAuthorityTier(safe.authority);return Object.freeze({playerId:safe.playerId,authority:safe.authority,matches:safe.matches,byMode:Object.freeze({...safe.byMode}),level:progression.level,tier:progression.tier,progress:progression.progress,progressRequired:progression.progressRequired,nextAuthority:progression.nextMin});}
