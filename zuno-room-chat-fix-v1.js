(()=>{if(window.__ZUNO_ROOM_CHAT_FIX_V1__)return;window.__ZUNO_ROOM_CHAT_FIX_V1__=true;const MAX_MESSAGES=60;let observer=null,raf=0;function qs(s){return document.querySelector(s)}function trimAndScroll(){const box=qs('#messages');if(!box)return;const rows=[...box.querySelectorAll('.chat-row')];if(rows.length>MAX_MESSAGES)rows.slice(0,rows.length-MAX_MESSAGES).forEach(el=>el.remove());box.scrollTop=box.scrollHeight}function schedule(){cancelAnimationFrame(raf);raf=requestAnimationFrame(trimAndScroll)}function init(){const box=qs('#messages');if(!box)return;trimAndScroll();observer=new MutationObserver(schedule);observer.observe(box,{childList:true,subtree:false});window.addEventListener('resize',schedule,{passive:true});window.addEventListener('orientationchange',schedule,{passive:true});window.addEventListener('zuno:room-app-ready',schedule)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();window.addEventListener('pagehide',()=>{observer?.disconnect();cancelAnimationFrame(raf)})})();