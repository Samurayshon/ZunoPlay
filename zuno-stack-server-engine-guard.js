(()=>{
if(window.__ZUNO_STACK_SERVER_ENGINE_GUARD__)return;window.__ZUNO_STACK_SERVER_ENGINE_GUARD__=true;
const q=new URLSearchParams(location.search),roomId=q.get('room')||sessionStorage.getItem('zunoplay_room_id')||'';
if(!/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(roomId||''))return;
const safeKinds=new Set(['start','tile','undo','relay_send','relay_take','pulse_shift']);
let wrapped=false,timer=0,lastNotice=0;
function toast(t){const e=document.getElementById('toast');if(!e)return;e.textContent=t;e.classList.add('show');clearTimeout(e._t);e._t=setTimeout(()=>e.classList.remove('show'),1500)}
function notice(){const now=Date.now();if(now-lastNotice<1200)return;lastNotice=now;toast('Recurso avançado suspenso no coop até validação server-side.')}
async function resync(reason){try{return await window.ZunoStackAuthority?.reconcile?.(reason)}catch(_){return false}}
function hookAuthority(){const a=window.ZunoStackAuthority;if(!a||wrapped||typeof a.commit!=='function')return false;const original=a.commit.bind(a);a.commit=async(kind,engine)=>{const k=String(kind||'mutation');if(k==='late_start'){await resync('late_start_server_required');return false}const active=!!window.ZunoStackCore?.getState?.()?.active;if(active&&k==='hint')return window.ZunoStackHintAuthority?.request?.()??false;if(active&&!safeKinds.has(k)){await resync(`server_action_required_${k}`);return false}if(active&&k==='pulse_shift')return window.ZunoStackPulseAuthority?.shift?.()??false;const ok=await original(k,engine);if(ok===false&&active&&!safeKinds.has(k))await resync(`server_action_rejected_${k}`);return ok};wrapped=true;document.documentElement.dataset.zstackServerEngine='locked';resync('server_engine_guard_ready');return true}
function capture(e){const target=e.target instanceof Element?e.target:null;if(!target||!window.ZunoStackCore?.getState?.()?.active)return;const pulse=target.closest('#pulse'),hint=target.closest('#hint'),power=target.closest('[data-zso-tactical],[data-zso-power]');if(!pulse&&!hint&&!power)return;e.preventDefault();e.stopImmediatePropagation();if(e.isTrusted===false)return;if(pulse){const fn=window.ZunoStackPulseAuthority?.shift;if(typeof fn==='function')fn();else{toast('Pulse aguardando autoridade do servidor.');resync('pulse_authority_pending')}return}if(hint){const fn=window.ZunoStackHintAuthority?.request;if(typeof fn==='function')fn();else{toast('Dica aguardando autoridade do servidor.');resync('hint_authority_pending')}return}notice();resync('power_server_required')}
function boot(){document.addEventListener('click',capture,true);hookAuthority();window.addEventListener('zuno:stack-authority-ready',hookAuthority);timer=setInterval(()=>{if(hookAuthority()){clearInterval(timer);timer=0}},150);setTimeout(()=>{if(timer){clearInterval(timer);timer=0}},10000)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
window.addEventListener('pagehide',()=>{document.removeEventListener('click',capture,true);if(timer)clearInterval(timer)},{once:true});
})();