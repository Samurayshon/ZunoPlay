# Beta B0 Vercel Preview Probe

Purpose: trigger a non-production GitHub branch/PR event so the existing Vercel Git integration can expose a Preview deployment for Gate B0 validation.

This file has no runtime effect and must not be merged as product functionality.
