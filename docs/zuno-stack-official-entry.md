# Official entry

`jogos.html` exposes one Zuno Stack card and routes it to `zuno-stack.html`.

`zuno-stack.html` is the official runtime shell and loads the validated Solo UI module and stylesheet. The old validation URL redirects to this official entry.