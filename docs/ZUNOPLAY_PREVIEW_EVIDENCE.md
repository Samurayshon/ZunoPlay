# Evidências exigidas — ZUN-47

O fechamento desta etapa requer registrar evidências observáveis, não apenas código:

- identidade e isolamento do Supabase staging;
- inventário/segurança relevante do backend;
- CI da PR;
- identidade do projeto Vercel;
- URL real do deploy Preview;
- logs de build/deploy;
- smoke do frontend hospedado;
- confirmação de que o deploy GitHub Pages de produção continua saudável;
- confirmação de que o Android continua com o origin canônico.

Sem URL/logs/smoke do Preview, a etapa não é VALIDADA.
